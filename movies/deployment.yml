apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: movies
  name: movies
  namespace: movies
spec:
  replicas: 3
  selector:
    matchLabels:
      app: movies
  strategy: 
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: movies
    spec:
      containers:
      - image: siddharth67/movies-ui:ngnix
        name: movies
        imagePullPolicy: Always
        ports: 
        - containerPort: 80
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "50m"
            memory: "128Mi"
        readinessProbe:
          httpGet:
            path: /
            port: 80        
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80            
          initialDelaySeconds: 30
          periodSeconds: 10
